import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFFF4D4D); // Red gradient background color
  static const Color primary = Color(0xFFFF4D4D);    // Red color for button
  static const Color white = Color(0xFFFFFFFF);      // White color for text and inputs
  static const Color textFieldBg = Color(0xFFF2F2F2); // Light grey background for text fields
  static const Color textGrey = Color(0xFF888888);   // Grey color for placeholder text
  static const Color black = Color(0xFF000000);      // Black for text
  static const Color forgotPassword = Color(0xFFFF4D4D);  // Red color for "Forgot Password?"
}
